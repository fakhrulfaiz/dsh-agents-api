# Package README reshape (2026-09-18)

## Summary

The package README follows the DeepSeek Harness `package-bundle` template: profile install first, folded implementation, Model Experience, and Known Limitations. HTTP route tables and wire detail moved to [`http-api.md`](../http-api.md) so the README stays the install and contract entry point.

## Path correction

Local install uses `./packages/experimental/dsh-agents-api` (the directory name in this tree).
